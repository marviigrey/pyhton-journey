This is a folder to record my python journey. learning python to know how to code and do lots of fun stuffs.

Day 1. basically installing and setting up python, understanding the extension and how to run python on command line.
Declaring variable:
e.g 
name = 'marv'
name000='test'

There are some reserved keywords you can't use in python, some of them are for,if etc. You can't use them to assign variable names.

Comments: we make use of comments to give information about of our code.

Take notes of indentation, python doesn't ignore white spaces.

Python operators:
symbols used to perform operations on values and the variables that hold those values.
+,-,*, and / are common arithemetics operation
we can do
comparison operator:
  ==
  !=
  >
  <
  >=
  <=

 Data types

 we have strings, numbers
 concatenate: joining multiple data types using arithmetic operator.

 To create a string or text with multiple lines, make use of:
    ''' string value
        string value
        string value
        string value
        string value
'''

Escaping special characters we make use of '\' (backword slash)
e.g 
    sentence = 'I\'m back at work!\tHey!\n\nWhere\'s this at\\located'
    print(sentence)

List of some important string functions:
lower() - prints in lower case
upper() - prints in uppercase
replace("<old_value>", "<new_value>") - replace an old value with a new one.
str(number) - Casting a number to a string
type() - get the data type of a value
title() - replace to a title format,mostly in uppercase.
strip() - take off white spaces
lstrip() - take off white spaces to the right
rstrip() - take off white spaces from the right.
ljust()
rjust()
center()
startswith()
endswith()







